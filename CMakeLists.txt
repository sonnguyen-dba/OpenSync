# OpenSync/CMakeLists.txt
cmake_minimum_required(VERSION 3.16)
project(OpenSync VERSION 1.0.0 DESCRIPTION "OpenSync, Consume data from topic by produce from @OpenLogReplicator to databases" LANGUAGES CXX)

add_subdirectory(src)
